<div class="flex justify-center">
    @if($data)
        <span class="inline-flex items-center px-2 py-0.5 rounded-sm font-medium leading-5 bg-primary-100 text-primary-800">
            {{ $data }}
        </span>
    @endif
</div>
